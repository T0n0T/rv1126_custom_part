#ifndef YAML_CONFIG_H
#define YAML_CONFIG_H

/* Minimal generated-config replacement for the static vendored build:
 * yaml_private.h includes "config.h" when HAVE_CONFIG_H is defined. Kept in
 * this directory so the quoted include resolves here first, before any SDK
 * config.h on the -I path. */
#define YAML_VERSION_MAJOR 0
#define YAML_VERSION_MINOR 2
#define YAML_VERSION_PATCH 5
#define YAML_VERSION_STRING "0.2.5"

#endif /* YAML_CONFIG_H */
